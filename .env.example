# Required for production builds (`next build`). Use a long random string (no "/").
# Example: openssl rand -hex 16
# REPORT_ACCESS_TOKEN=

# Optional: base URL for QR generation script (default https://floratrace.com)
# REPORT_BASE_URL=https://floratrace.com
